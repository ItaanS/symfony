{% extends 'base.html.twig' %}

{% block title %}Show Actors 01{% endblock %}

{% block body %}
<pre>
    {{ dump() }}
</pre>
<h1>Actor</h1>
<table class="table">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ actor.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ actor.name }}</td>
        </tr>
        {% for serie in actor.programm %}
        <tr>
            <th>
                <td>{{ serie.title }}</td>
            </th>
            <td></td>
        </tr>
        
        {% endfor %}
    </tbody>
</table>

<a href="{{ path('actor_index') }}">back to list</a>

<a href="{{ path('actor_edit', {'id': actor.id}) }}">edit</a>

{{ include('actor/_delete_form.html.twig') }}
{% endblock %}

{# <strong>Acteurs :</strong>
<ul>
    dump
    {% for actor in programm.actors %}
    <li>{{ actor.name }}</li>
    {% else %}
    <li>Aucun acteur pour cette série</li>
    {% endfor %}
</ul> #}