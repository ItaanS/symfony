{% extends 'base.html.twig' %}

{% block title %}Season Show{{ program.id }}{% endblock %}

{% block body %}
<pre>
    {{ dump() }}
</pre>
<div class="media d-flex justify-content-center">
    <div class="media-body d-flex flex-column align-items-center">
        <h1>{{ program.title }}</h1>
        <h2>Liste des saisons</h2>

        {% for episode in seasons.episodes %}
        <p>{{ loop.index }} / {{ episode.title }}</p>
        <a href="{{ path("program_episode_show", { programId: program.id, seasonId: seasons.id,
            episodeId: episode.id}) }}">Voir épisodes</a>
        {% endfor %}
        <p></p>

    </div>
</div>
{% endblock %}